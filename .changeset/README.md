# Changesets

버전 관리/릴리즈는 Changesets로 합니다.

## Quick start

```bash
# 변경사항 기록(새 changeset 생성)
bun run changeset

# packages/* 버전 반영(package.json 업데이트)
bun run version-packages

# (선택) npm publish
bun run release
```

